# Test framework
# We pinned pytest to 2.x because of an apparent incompatibility between pytest>=3 and pytest-django. We used the
# same version in mvne-platform at the time of writing. For more context, see our Slack history at
# https://vikingco.slack.com/archives/backend/p1474619771001212
pytest==2.8.3
pytest-django==2.9.1
pytest-cov==2.3.1

# Coverage uploads
coveralls
coverage==3.7.1
docopt==0.6.2
requests==2.9.1

# Mocking, faking, factories and other test helpers
mock==1.3.0
fake-factory==0.6.0
factory-boy==2.6.1
django-factory-boy==1.0.0

# For provider_kbc
beautifulsoup==3.2.1

# Flake8 checks for PEP-8 formatting and code quality
flake8==2.5.2
pyflakes==0.9.2
pep8==1.6.2
# Checks for cyclomatic complexity
mccabe==0.3.1
# Checks for naming conventions
pep8-naming==0.3.3
# Checks for the use of print statements
flake8-print==1.6.1
# Checks for the use of debug statements (pdb, ...)
flake8-debugger==1.4.0
# Checks for the overriding of builtins
-e git+ssh://git@github.com/vikingco/flake8-builtins.git@1.0#egg=flake8-builtins
# Checks for uses of Django-style asserts
-e git+ssh://git@github.com/vikingco/flake8-pytest.git@1.1#egg=flake8-pytest
# Checks for module-level imports
git+ssh://git@github.com/vikingco/flake8-module-import.git@1.0#egg=flake8-module-imports
# Checks for usages of django.translation.activate()
git+ssh://git@github.com/vikingco/flake8-translation-activate.git@1.0.1#egg=flake8-translation-activate
# Checks for imports with an alias of _
git+ssh://git@github.com/vikingco/flake8-ugettext-alias.git@1.0#egg=flake8-ugettext-alias
